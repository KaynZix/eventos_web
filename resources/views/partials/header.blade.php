  <header class="bg-gray-900 text-white py-4 shadow-md">
    <div class="container mx-auto flex justify-between items-center px-4">
      <h1 class="text-2xl font-bold">Eventos</h1>
      <nav class="space-x-6">
        <a href="{{ route('welcome')  }}" class="hover:text-gray-300">Inicio</a>
        <a href="{{ route('welcome')  }}" class="hover:text-gray-300">Eventos</a>
        <a href="{{ route('asientos')  }}" class="hover:text-gray-300">Reservas</a>
        <a href="{{ route('welcome')  }}" class="hover:text-gray-300">Contacto</a>
      </nav>
    </div>
  </header>